# AI-nimator — Questions ouvertes pour un talk (décisions à trancher)

> **But** : regrouper les points qui **ne peuvent pas** être tranchés en
> édition de doc seule et qui nécessitent une discussion synchrone avec
> l'utilisateur. Chaque entrée = contexte court, options, et ce qui est **bloqué**
> tant que ce n'est pas décidé. Une fois tranché : reporter la décision dans
> la fiche concernée (avec date) et retirer l'entrée ici.
>
> Créé le 2026-06-24. Source : revue de cohérence des ROADMAP.


---

## Q-4 ajout du hub pour le train controler v2

---

## Q-3 probleme du prompt

---

## Q-2 — face tracking

**questions utilisateur**: comment ajouter du face traking ? quelle est la meilleur approche ?

**context**: je ne veut pas que ce soit trop lourd comment l'integrer ?  same / another model ? dataset ?

---

## Q-1 — creation du DataSet

**questions utilisateur**: comment bien construire un dataset ? si je fait mon dataset "from scratch" (ce qui devra etre fait si je decide de comercialiser le produit), comment proceder ? comment savoir si j'ai tout ? comment evaluer le "coverage" du dataset ? comment ne pas l'over engeneer (10k  sample de marche pour 5 samples d'attaques ... on a deja trouver mieux ) ?

j'aimerais aussi pouvoir entrainer l'IA a reproduire ce qu'elle voie sur une video (corelation avec le LoRa). example produit : un developeur indé decide de faire une cinematique. 1 camera + ma solution => aniamtion 3D cree

je pensais a un VAE qui prend une video en entree "interchangeable" avec la layer de text

**context**: en te basent sur toutes les sources (pour l'environement model "target")

---

point 0: negatifs = questions perso, positifs = tes questions
