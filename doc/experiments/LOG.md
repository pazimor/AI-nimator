# Experiments LOG

One line per concluded experiment. Format:
`| date | run | config (delta vs default) | metrics | verdict |`

| Date | Run | Config | Metrics | Verdict |
|---|---|---|---|---|
| 2026-06 | scaling sweep N=5/20/100/400 (`scaling_curve_v2.py`) | constant step budget, ACCAD, frozen CLIP | fidelity N=100: −0.151, retrieval 0.20, distinct +0.394 | Confounded (exposure 700→56) — cliff was under-training, see decisive test |
| 2026-06-07 | `n100_decisive` (600 exposures/sample) | same 100 samples/probes as sweep | fidelity recovered (vs −0.151 @56 exp) | Under-training confirmed as the N=100 cliff cause |
| 2026-06-07→10 | `n500`, `n500e600`, `n500_big`, `n1000_big`, `n1000_cfg` | capacity ladder probes | consolidated into the capacity law below | Intermediate points: 512/6 (~45M) holds ~N≤500; N=1000 needs more |
| 2026-06-12 | `n1000_640` (640d/8L, ~90M params) | N=1000, cond-mask-prob 0.10, grad-accum 1, no epoch cap, ~600 exp/sample, frozen CLIP (diagnostic encoder) | cfg=1: fid +0.648 / ret 0.20 / dist +0.832 — cfg=4: +0.591 / 0.80 / +0.339 — cfg=6: +0.607 / **1.00 (5/5)** / +0.034 | ✅ **640/8 solves N=1000.** CFG 4–6 essential at inference (model contains the conditioning, CFG expresses it) |
| 2026-06-12 | **Capacity↔N law (3 clean points)** | 384/4 ~17M → N≤~50 ; 512/6 ~45M → N≤~500 ; 640/8 ~90M → N=1000 ✓ | — | **params ≈ ∝ N** (~double params when N doubles) — measured in memorization regime (train==val); generalization bend unknown → step B1-ter |
| 2026-06-16 | `b1bis_n1000_640` (B1-bis consolidation, `scripts/consolidate_n1000_v2.py`, 47h) | N=1000 **random across 5 folders** (KIT 43%), 640/8, **cond-mask-prob 0.10**, frozen CLIP, 600 epochs, 24 held-in + 24 held-out probes | held-in cfg6: fid +0.354 / ret 0.29 / dist +0.196 ; 5-probe cfg6: fid +0.689 / ret 0.40 / dist +0.557 ; held-out ≈ chance. diagnose: **cfg_sim 0.971, seed_sim 0.966** (both fail), encoder cond↔uncond 0.305 (OK), root_displ=0 | ⚠ **Did NOT reproduce June.** Fidelity OK but denoiser-level conditioning collapse (mode-collapse on seed, CFG near-dead). Root cause hypothesis: cond-mask-prob 0.10 too low on diverse multi-folder N (code documents 0.10→cfg_sim≈1.0 on 5 folders; 0.20 is the fix). Loss plateaued ~epoch 100-150 (600 was ~3-4× wasteful). Next: re-run cond-mask 0.20, early-stop ~200 |
| 2026-06-17 | **Orientation root cause + fix** (ai-debugging) | preprocessed data not Y-up: AMASS Z-up root_orient applied to Y-up rest skeleton, no transform → bodies stored lying down. Fix `canonicalizeMotionUpright` (frame-0 hips+spine → Y-up/+Z), wired into preprocess `--canonicalize-facing` | 25-30/30 upright on real data; 3 unit tests | ✅ **Confirmed by Blender** (GT + gen now upright, not T-pose). FK metrics were degraded by mis-orientation. |
| 2026-06-18 | `canon_n1000` (3 folders ACCAD/BMLmovi/BioMotionLab, canonicalised, 200 ep, cond-mask 0.20, vel-xyz 1.0, frozen CLIP) | 22 held-in + 22 held-out probes | held-in cfg6: **fid +0.747** / ret 0.18 / dist +0.695 (fid was +0.20 before orientation fix). diagnose cfg_sim 0.985, seed_sim 0.986, cond↔uncond 0.289. Foot stride z=0.43 vs GT 0.91. | ✅ **Orientation fix validated** (upright in Blender, fidelity jump). Motion looked muted → triggered a diversity investigation. |
| 2026-06-18 | **rot6d jerk loss = working trembling lever** | trembling is in rot6d space (FK-joint losses can't see it). Added `rotationJerkLossV2` (Δ² on rot6d) + config `rotationJerkWeight`. Overfit-8 framevel sweep: w0=0.094, w20=0.069, **w50=0.054 (stride 0.82 ✓)**, w150=0.041 (stride 0.49, over-smooth) | ✅ First lever to cut trembling while keeping amplitude. **Recipe: rotationJerkWeight≈50 + smooth-sigma 1.0 at inference.** Wired into consolidate script (--rotation-jerk-weight, default 50) + 3 regression tests. |
| 2026-06-18 | **Trembling BUG found + fixed** | `temporalDifference` defaults to dim=0 (batch) but FK is (B,F,bones,3) → time=dim1. `velocityXyzLossV2` + `footContactLossV2` differenced the BATCH, not time → smoothness/anti-skate never supervised | fix dim=1 in velocity/foot/accel; add `accelerationXyzLossV2` (jerk) + configurable `velocitySchedule`; verified temporal + jitter-penalising, lint+tests green | ✅ **Root cause of trembling = a real bug, not a hyperparameter.** Probe (overfit-8) to confirm gen framevel ↓ before full retrain. MDM gap noted: foot-contact off + heavy contrastive (61%) starves reconstruction. |
| 2026-06-18 | **Diversity investigation — 4 levers ruled out + REFRAME** | tested clip-guidance 0.1, sampler eta {0,0.5,1}, Min-SNR-γ {5,0} | none moved seed_sim (~0.96-0.99). GT intra-prompt-cluster sim: swaying 0.962, sways-LR 0.981 → **data isn't diverse per prompt**, so high seed_sim is correct, not collapse. Averaging cluster walks cuts amplitude only ~25-35%; gen stride 0.43 ∈ real-walk range 0.34-0.40. "Barely moves" was mostly **smooth-sigma 2.0 over-damping** (0.43→0.22). Overfit-8 → full amplitude (machinery OK). | ⚠ **"Collapse/diversity" was a misframing**; `retrieval`/`seed_sim` misleading under 72% prompt ambiguity. Real win = orientation. Judge motion BY EYE (smooth-sigma ≤1.0), not FK retrieval. Next: lock orientation (full re-preprocess), train, visual eval. |

| 2026-06-21 | **MDM-vs-ours gap analysis (static motion @ N=1000)** | code review only (losses_v2.py, full_training_v2.py, consolidate_n1000_v2.py, network.yaml). User benchmark: MDM succeeds on the SAME data → not primarily a data problem. | Ranked structural gaps: (1) loss-mix starves motion — `clipGuidanceWeight=0.6` dominates ~61% of loss (docstring admits it "mutes motion"); FK/jerk losses faded at high-t via `velocitySchedule="timestep"` → vel/joint share < 1% (§3.5 = dead) and `velocityXyzWeight=0.0` by default in the run driver; (2) we predict **v**, MDM predicts **x0** (sample) + applies its geometric losses in x0 space at every t — v-pred + Min-SNR-γ=5 down-weights low-noise (final-frame detail) where motion lives; (3) MDM supervises joint-position + velocity + foot-contact at ALL noise levels, we gate them behind ᾱ_t (high-t fade) and default most to 0. | ⚠ **Provisional** (pending request 002 loss-share + motion-energy data). Recommendation: cheapest first diagnostic = re-run N=1000 (≤200 ep) with loss-mix rebalanced toward reconstruction/motion, NOT a new loss term: `--clip-guidance-weight 0.1 --vel-xyz-weight 1.0` + `velocitySchedule="none"` (supervise smoothness at all t, MDM-style) + jointPositionWeight 1.0. All are config/CLI overrides; the `velocitySchedule` default flip and any default change need Pazimor's sign-off. |
| 2026-06-21 | **N=1000 "correctly pass" audit + orchestrator patch** | inspected most-recent run `output/recipe_n1000` (gens dated today): resolved_config = `dataset_preprocessed_canon` (UPRIGHT ✓), 640/8, cond-mask 0.20, but the OLD starving mix — `clipGuidanceWeight=0.6`, `velocityXyzWeight=0.0`, `jointPositionWeight=0.0`, `velocitySchedule=timestep`. Found `consolidate_n1000_v2.py` could NOT express the LOG-2026-06-21 rebalance: it never passed `velocitySchedule`/`jointPositionWeight`/`footContactWeight`, so `--vel-xyz-weight 1.0` was still faded ~0 at high t. | ✅ **The rebalanced recipe has not yet been run** — `recipe_n1000` is the starving baseline. Patched the orchestrator (new flags `--velocity-schedule` / `--joint-position-weight` / `--foot-contact-weight`; dataset default → upright canon). No `src/` default touched. Next: smoke (`--epochs 5`) then the ≤200-ep rebalanced run; flipping the `velocitySchedule` **default** in `src/` stays Pazimor's call. |
| 2026-06-22 | `rebalance_n1000` (640/8, cond-mask 0.20, `--clip-guidance-weight 0.1 --vel-xyz-weight 1.0 --velocity-schedule none --joint-position-weight 1.0 --rotation-jerk-weight 50`, upright canon) — **killed at epoch ~120/200** after user reported loss "yoyo + high peaks, plateau ~ep30" | `health.jsonl` (240 rec): `loss_clip_guidance` min0.39/max**4.98**/mean1.41/std0.73 vs `loss_bone` mean0.089/std0.063 (stable, low); `loss_total` max3.27 tracks clip. Loss share STILL **73% contrastive** (clip 34% + aux_pool 40%) despite weight 0.6→0.1 — `aux-pool-weight` left at 0.5 (untouched) + clip raw magnitude ~1.4 dwarfs geometric raw ~2e-4; vel_xyz/joint_xyz shares <1% (§3.5 = dead). `verdict.loss_decomposition=CRITICAL`. At ep120: `conditioning_sensitivity=0.10` (low), `effective_rank=0.019` (collapsed). 3 distinct prompts (walk/jump/wave, cfg4, sigma2.0) → motion energy identical to 0.5% (0.4466/0.4497/0.4494). | ⚠ **Two findings.** (1) The loss "yoyo" = CLIP **contrastive variance at batch 8** (in-batch negatives) — curve noise, NOT divergence; `loss_total` is not a convergence signal here. (2) Real problem = **conditioning / mode collapse** (effective_rank 0.019, cond_sens 0.10, prompt-agnostic motion energy) — a loss-mix flip alone won't fix it; the contrastive domination (73%) is the likely *driver* of the collapse. Relaunch corrected: `--clip-guidance-weight 0.02 --aux-pool-weight 0.0` (+ same geometric levers). **Early kill-criterion @ ep20–30:** watch `effective_rank`↗ / `conditioning_sensitivity`→0.3+; if still collapsed without the contrastive, lever is elsewhere (capacity or `cond-mask-prob 0.20` starving conditioning) — stop, don't burn 12h. |
| 2026-06-22 | `rebalance_n1000_v2` (640/8, cond-mask 0.20, `--clip-guidance-weight 0.02 --aux-pool-weight 0.0 --vel-xyz-weight 1.0 --velocity-schedule none --joint-position-weight 1.0 --rotation-jerk-weight 50`) — **ran full 200 ep**; user: "only first ~20 ep descend" | Loss-mix **fixed**: shares bone 34% + rotation_jerk 41% + global 10% + clip **15%** (was 73%). BUT collapse **persists, flat ep0→199**: `effective_rank` 0.016→0.020 (§3.5 CRITICAL <0.1), `conditioning_sensitivity` 0.00→0.046 (§3.5 CRITICAL ≈0 persistent = "prompt sans effet"). Double-check ruled out two causes: (a) frozen-CLIP NOT degenerate — pooled cross-prompt cosine **mean 0.65** (walk·sit 0.33, jump·kick 0.91), encoder differentiates prompts; (b) conditioning arch ON (resolved_config: FiLM + per-block FiLM + null-embedding all true). vel_xyz/joint_xyz STILL <1% (raw magnitude ~2e-4, weight 1.0 can't lift them; rotation_jerk carries the smoothness at 41%). | 🔴 **Encoder + loss-mix + arch all cleared → the DENOISER ignores the conditioning** (generates a prompt-agnostic average; persists with AND without contrastive). cond-mask-prob ruled out as the lever (both 0.10 and 0.20 collapse). **Decision (Pazimor): test the capacity hypothesis** — 640/8 may be too small to memorise 1000 *distinct* motions at N=1000, so it averages. Next run: bump denoiser (768/10 first rung) via new orchestrator flags `--denoiser-embed-dim/--denoiser-num-layers/--denoiser-num-heads` (defaults 640/8/8, no `src/` default touched). Kill-criterion @ ep~40: if `conditioning_sensitivity` hasn't climbed clearly above ~0.05 baseline (toward 0.15+), capacity isn't it — stop. |
| 2026-07-01 | **Diagnostic génération contrôleur « walking »** (`controller_text_full`, entraîné 2026-06-26 @3f3f402, généré via `generate_controller_v2`) | resolved_config du run : `promptEmbChannels: 0` (le profil `full` de `network.yaml` n'active pas le texte — seul `controller_text`, variante overfit, le fait) ; `scheduledSampling: 0.0` | Segment guidé (contrôle GT-derived) **statique** ; rollout libre en **dérive continue impossible**. Causes : (1) `generate_controller_v2` ignore **silencieusement** le prompt quand le checkpoint a `promptEmbChannels=0` → retombe sur l'embedding nul → motion moyenne ; (2) aucun scheduled sampling → biais d'exposition non mitigé, erreurs de delta cumulées dans l'intégration monde. Aucun `health.jsonl` : la voie contrôleur n'était pas branchée au flux santé. | 🔴 **Run full invalidé pour le texte** — re-pass **A6-bis** requis (profil full avec texte + SS>0, gated Pazimor, cf. ROADMAP_DETERMINIST A6). ✅ Corrigé ce jour : `ControllerHealthWriter` branche les deux boucles contrôleur sur `health/health.jsonl` (moniteur opérationnel pour la voie produit) ; le silent-ignore du CLI de génération devra devenir une **erreur explicite** (spec A6-bis). |

| 2026-07-02 | **Diagnostic « le contrôleur ne réagit pas au prompt » (`controller_full_text_a6bis`)** | run full A6-bis 2026-07-02 (promptEmbChannels=768, SS=0.3, encoder `output/xlm_roberta_artifact`) ; génération OK mécaniquement (prompt encodé, plus de silent-ignore) mais mouvement insensible au texte | Sonde encodeur : cosinus inter-prompts xlm_roberta_artifact = **0.995–0.998** (walk vs sit vs jump indiscernables) → signal de conditionnement texte **constant**, le modèle ne peut pas apprendre à réagir. Cause amont : `ClipTextEncoder` chargeait le backbone via `AutoModel` → un nom CLIP retourne le CLIPModel complet (forward exige `pixel_values`, crash) ; xlm-roberta avait été choisi pour contourner, mais ses embeddings pooled sont anisotropes (dégénérés sans fine-tuning contrastif). | 🔴 **Run 2026-07-02 invalidé pour le texte (encodeur dégénéré), re-train requis.** ✅ Fix : `ClipTextEncoder` prend la tour texte (`.text_model`) d'un checkpoint CLIP ; nouvel artefact canonique `output/clip_text_artifact` (clip-vit-base-patch32, 512) → cosinus inter-prompts **0.66–0.75** (réf. LOG juin 0.65). Profils `full`/`controller_text` alignés sur 512. xlm_roberta_artifact à ne plus utiliser. |

## Validated recipe (2026-06-12)
- Denoiser capacity ∝ number of distinct motions (double params when N doubles).
- `cond-mask-prob 0.10` (else degenerate collapse + no usable CFG).
- **CFG 4–6 at inference** — mandatory at scale (cfg=1 → retrieval 0.20, cfg=6 → 1.00).
- Sufficient exposure: grad-accum 1, no `maxSamplesPerEpoch` cap, ~600 exposures/sample.

Root cause of the original mystery ("model ignores the prompt at scale"):
under-sized denoiser AND sampling without CFG. Both fixed.
