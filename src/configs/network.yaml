# ============================================
# Network Architecture Configuration
# ============================================
# This file defines the neural network architecture for both
# CLIP and Generation models. The top-level embed-dim configures
# CLIP, while generation.embed-dim can override the denoiser width.
# ============================================


# ============================================
# Profile: Mac (32GB RAM, MPS GPU)
# Default profile for local development
# ============================================
default:
  # CLIP embedding dimension (must match the CLIP checkpoint output size)
  embed-dim: 512
  
  # CLIP Motion Encoder Architecture
  clip:
    motion-num-heads: 4
    motion-num-layers: 2
  
  # Generation Denoiser Architecture
  generation:
    # Internal denoiser width (can be larger than the CLIP embed-dim)
    embed-dim: 768
    num-heads: 4
    num-layers: 2
    num-bones: 22
    diffusion-steps: 500
    num-spatial-layers: 1
    num-hierarchy-layers: 1
    num-spatio-temporal-layers: 4

# ============================================
# Profile: DGX Spark (128GB RAM, GB10 GPU)
# Use with: --profile spark
# ============================================
spark:
  embed-dim: 512
  
  clip:
    motion-num-heads: 8
    motion-num-layers: 4

  generation:
    embed-dim: 512
    num-heads: 8
    num-layers: 8
    num-bones: 22
    diffusion-steps: 500
    num-spatial-layers: 2
    num-hierarchy-layers: 2
    num-spatio-temporal-layers: 2
