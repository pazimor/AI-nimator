paths:
  dataset-root: "/Volumes/oui/dataset_used/dataset"
  clip-checkpoint: "output/checkpoints/best_model.pt"
  checkpoint-dir: "output/generation_checkpoints"

training:
  batch-size: 2
  learning-rate: 0.0001
  epochs: 500
  embed-dim: 64  # Must match CLIP checkpoint
  num-heads: 6
  num-layers: 4
  num-bones: 22  # Number of skeleton bones in the dataset
  diffusion-steps: 1000
  device: cpu
  max-length: 64
  model-name: xlm-roberta-base
  validation-split: 0.1
  early-stopping-patience: 5
  # resume-checkpoint: output/generation_checkpoints/best_model.pt
